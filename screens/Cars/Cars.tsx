import React from "react";
import { Text, View } from "react-native";

export const CarsScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Cars List Screen</Text>
    </View>
  );
};

export default CarsScreen;
